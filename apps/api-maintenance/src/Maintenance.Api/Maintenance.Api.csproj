<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <!-- Local secrets (Jwt:SigningKey) live in `dotnet user-secrets`, never in appsettings.json. -->
    <UserSecretsId>maintenance-api-2026</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Maintenance.Application/Maintenance.Application.csproj" />
    <ProjectReference Include="../Maintenance.Infrastructure/Maintenance.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
    <PackageReference Include="Swashbuckle.AspNetCore" />
    <!-- Design-time only: lets `dotnet ef` use this project as the start-up project. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Maintenance.IntegrationTests" />
  </ItemGroup>

</Project>
