# PrimeNG 21.1.9 (the last MIT release) declares Angular 21 peers but runs on Angular 22, verified by the
# full suite (S37). Let npm accept the range instead of failing on ERESOLVE.
legacy-peer-deps=true
