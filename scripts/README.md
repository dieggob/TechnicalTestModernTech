# scripts

Repository-level Bash scripts: `setup.sh`, `dev.sh`, `test.sh` (slice S05), `migrate.sh` (S06), `generate-api-client.sh` (S16), `compose-up.sh` and `compose-down.sh` (S34). Each delegates to the per-app commands described in `.claude/CLAUDE.md`.
